import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import { Button } from '@onesneakymofo/test-button'

<Meta title="Components/Button" component={Button} />

# Button

Use `<Button>` as the standard button used from the Landing design system. Toggle different variants and sizes to see the combinations of buttons that can be created and used.

export const Template = (args) => <Button primary>SampleText</Button>

<Canvas>
  <Story name="Button">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Button" />
